"use client";

import Link from "next/link";
import { ArrowUpDown, ChevronLeft, ChevronRight, Image as ImageIcon, MoreHorizontal, SearchX } from "lucide-react";
import type { PageResponse, Product } from "@/lib/types";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from "@/components/ui/dropdown-menu";
import { StatusBadge } from "@/components/products/status-badge";
import { getAssetUrl } from "@/lib/utils";

export function ProductTable({ page, categoryNames, onPageChange, onSort, sortField, sortDirection }: { page?: PageResponse<Product>; categoryNames?: Record<string, string>; onPageChange: (page: number) => void; onSort: (field: "name" | "brand" | "createdAt") => void; sortField: string; sortDirection: "asc" | "desc" }) {
  if (!page || page.content.length === 0) return <div className="flex flex-col items-center justify-center px-6 py-16 text-center"><div className="mb-4 flex h-12 w-12 items-center justify-center rounded-2xl bg-slate-100 text-slate-500"><SearchX className="h-6 w-6" /></div><h2 className="text-base font-semibold text-slate-900">No products match your filters</h2><p className="mt-2 max-w-md text-sm leading-6 text-slate-500">Try a different search term or clear the category filter to see more of the catalog.</p></div>;
  return <><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left"><thead><tr className="border-b border-slate-100 text-xs font-semibold uppercase tracking-[0.1em] text-slate-400"><th className="px-5 py-4">Product</th><th className="px-3 py-4"><SortButton label="Brand" field="brand" active={sortField === "brand"} direction={sortDirection} onClick={onSort} /></th><th className="px-3 py-4">Category</th><th className="px-3 py-4">Status</th><th className="px-3 py-4">Variants</th><th className="px-3 py-4 text-right">Actions</th></tr></thead><tbody>{page.content.map((product) => <tr className="table-row" key={product.id}><td className="px-5 py-4"><Link href={`/products/${product.id}`} className="flex items-center gap-3"><div className="flex h-11 w-11 shrink-0 items-center justify-center overflow-hidden rounded-xl bg-slate-100 text-slate-400">{product.images[0] ? <img src={getAssetUrl(product.images[0].url) ?? ""} alt="" className="h-full w-full object-cover" /> : <ImageIcon className="h-5 w-5" />}</div><div className="min-w-0"><p className="truncate text-sm font-semibold text-slate-800 hover:text-primary">{product.name}</p><p className="mt-1 truncate text-xs text-slate-400">{product.slug}</p></div></Link></td><td className="px-3 py-4 text-sm text-slate-600">{product.brand || "—"}</td><td className="px-3 py-4"><Badge variant="muted">{categoryNames?.[product.categoryId] ?? "Unknown category"}</Badge></td><td className="px-3 py-4"><StatusBadge status={product.status} /></td><td className="px-3 py-4 text-sm text-slate-600">{product.variants.length}</td><td className="px-3 py-4 text-right"><DropdownMenu><DropdownMenuTrigger asChild><Button variant="ghost" size="icon-sm" aria-label={`Actions for ${product.name}`}><MoreHorizontal className="h-4 w-4" /></Button></DropdownMenuTrigger><DropdownMenuContent align="end"><DropdownMenuItem asChild><Link href={`/products/${product.id}`}>View details</Link></DropdownMenuItem><DropdownMenuItem asChild><Link href={`/products/${product.id}?edit=1`}>Edit product</Link></DropdownMenuItem></DropdownMenuContent></DropdownMenu></td></tr>)}</tbody></table></div><div className="flex flex-col gap-3 border-t border-slate-100 px-5 py-4 sm:flex-row sm:items-center sm:justify-between"><p className="text-xs text-slate-500">Showing <span className="font-semibold text-slate-700">{page.number * page.size + 1}–{page.number * page.size + page.numberOfElements}</span> of <span className="font-semibold text-slate-700">{page.totalElements}</span> products</p><div className="flex items-center gap-2"><Button variant="outline" size="sm" onClick={() => onPageChange(page.number - 1)} disabled={page.first}><ChevronLeft className="h-4 w-4" />Previous</Button><span className="px-2 text-xs font-medium text-slate-500">Page {page.number + 1} of {Math.max(page.totalPages, 1)}</span><Button variant="outline" size="sm" onClick={() => onPageChange(page.number + 1)} disabled={page.last}>Next<ChevronRight className="h-4 w-4" /></Button></div></div></>;
}

function SortButton({ label, field, active, direction, onClick }: { label: string; field: "name" | "brand" | "createdAt"; active: boolean; direction: "asc" | "desc"; onClick: (field: "name" | "brand" | "createdAt") => void }) { return <button className="inline-flex items-center gap-1.5 hover:text-slate-700" onClick={() => onClick(field)}>{label}<ArrowUpDown className={`h-3.5 w-3.5 ${active ? "text-primary" : ""}`} />{active && <span className="sr-only">sorted {direction === "asc" ? "ascending" : "descending"}</span>}</button>; }
