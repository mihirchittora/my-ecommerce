import { AlertTriangle, CheckCircle2, ClipboardList, Cuboid, MapPin, PackageSearch } from "lucide-react";
import type { InventorySummary } from "@/lib/types";
import { Card } from "@/components/ui/card";

export function InventorySummaryCards({ summary }: { summary?: InventorySummary }) { const cards = [{ label: "Total units", value: summary?.totalInventoryUnits, icon: Cuboid, tone: "blue" }, { label: "Available", value: summary?.availableUnits, icon: CheckCircle2, tone: "green" }, { label: "Reserved", value: summary?.reservedUnits, icon: ClipboardList, tone: "violet" }, { label: "Damaged", value: summary?.damagedUnits, icon: AlertTriangle, tone: "amber" }, { label: "Low-stock SKUs", value: summary?.lowStockSkus, icon: PackageSearch, tone: "rose" }, { label: "Active locations", value: summary?.activeLocations, icon: MapPin, tone: "slate" }] as const; const styles = { blue: "bg-blue-50 text-blue-600", green: "bg-emerald-50 text-emerald-600", violet: "bg-violet-50 text-violet-600", amber: "bg-amber-50 text-amber-600", rose: "bg-rose-50 text-rose-600", slate: "bg-slate-100 text-slate-600" }; return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-6">{cards.map(({ label, value, icon: Icon, tone }) => <Card className="p-5" key={label}><div className="flex items-start justify-between"><div><p className="text-sm font-medium text-slate-500">{label}</p><p className="mt-3 text-2xl font-bold tracking-tight text-slate-950">{value ?? "—"}</p></div><div className={`flex h-10 w-10 items-center justify-center rounded-xl ${styles[tone]}`}><Icon className="h-5 w-5" /></div></div></Card>)}</div>; }
